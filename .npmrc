engine-strict=true
save-exact=true
# The mailer package exposes preview/template engines as optional peers. Nexus
# sends raw HTML and does not use those packages; omitting them also keeps their
# vulnerable preview-only dependency tree out of production images.
omit=optional
